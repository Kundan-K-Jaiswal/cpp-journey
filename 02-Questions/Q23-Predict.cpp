// what will be the output ?

#include<iostream>
using namespace std;

int main() {
  int x = 3, y, z;
  x = y = 10;
  z = x < 10;
  cout << x << " " << y << " " << z << endl;
  // think of the output then press run to get output, don't worry about the syntax




























  cout << "for other operators if same precidence it will go *left to right* but for assignment operator it will go *right to left*.";
  return 0;
}